# Automation Console

Independent React/Vite console. Run `npm ci && npm run dev` against the local Java service or build the UI container. In the integrated stack it is linked from the AI-SDLC platform switcher. Configuration contains public Supabase settings only.
